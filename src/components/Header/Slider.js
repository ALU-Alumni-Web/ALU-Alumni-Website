import React from "react";
import classes from "./Slider.module.scss";

export const Slider = (props) => {
  return (
    <section className={classes.slider}>
      <h2>Jeremiah's Invention</h2>
    </section>
  );
};

export default Slider;
